{
  "name": "maps-website-builder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Self-hosted static sites for service businesses (Cloudflare Worker). Phase 0 spikes V3/V4.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "ingest": "tsx scripts/ingest.ts",
    "postcards": "tsx scripts/postcards.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "tsx": "^4.22.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "wrangler": "^3.78.0"
  },
  "dependencies": {
    "qrcode-generator": "^1.4.4"
  }
}
